package com.example.photosorter.ui.swipe

/**
 * Represents the four possible swipe directions for photo sorting,
 * plus [NONE] for when no swipe has been committed.
 */
enum class SwipeDirection {
    LEFT, RIGHT, UP, DOWN, NONE
}
